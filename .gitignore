# Build artifacts
__pycache__/
*.pyc
*.zip

# du-blueprint engine binary — shipped via GitHub Releases, not source control
bin/

# Novaquark game assets, extracted on the user's machine (never redistributed):
# element dimensions + icons are generated locally by "Extract DU Element Data".
icons/
data/

# Editor / OS junk
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
